{
  "name": "tebow-gateway",
  "description": "Holds the API key and the day's brief so the public console never has to",
  "main": "index.js",
  "engines": { "node": "20" },
  "dependencies": {
    "firebase-admin": "^12.6.0"
  },
  "private": true
}
